arr = [2, 7, 5]

perm_li = []
for j in range(len(arr)):
    for i in range(1,len(arr)+1):
        if arr[j:i+j] not in perm_li:
            perm_li.append(arr[j:i+j])
        
        
print(perm_li)
